# 核心依赖
streamlit==1.45.1
pandas==2.3.0
scikit-learn==1.4.0
numpy==1.24.4  # 降级到 1.x 以兼容 scikit-learn
Pillow==11.2.1  # 图片处理

# 必要工具库
setuptools>=60.0.0
protobuf>=6.31.1  # 升级到最新稳定版
pyarrow>=20.0.0  # 升级到最新稳定版
python-dateutil==2.9.0.post0
pytz==2025.2

# 其他显式依赖（仅保留你的代码直接调用的库）
tenacity==9.1.2
watchdog==6.0.0
