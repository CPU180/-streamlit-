# 核心依赖
streamlit==1.45.1
pandas==2.1.4
scikit-learn==1.4.0
numpy==1.26.0  # 最后一个稳定的 1.x 版本
setuptools>=70.0.0

# 必要工具库
setuptools>=60.0.0
protobuf>=6.31.1  # 升级到最新稳定版
pyarrow>=20.0.0  # 升级到最新稳定版
python-dateutil==2.9.0.post0
pytz==2025.2

# 其他显式依赖（仅保留你的代码直接调用的库）
tenacity==9.1.2
watchdog==6.0.0
