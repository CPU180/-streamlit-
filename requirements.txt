# 核心依赖
streamlit==1.45.1
pandas==2.2.2  # 明确支持 Python 3.13 的版本
scikit-learn==1.5.0  # 最新稳定版，兼容 Python 3.13
numpy==2.0.0  # 与 scikit-learn 1.5.0 兼容的版本
Pillow>=10.0.0  # 图片处理
setuptools>=70.0.0  # 确保包含 distutils 功能
# 必要工具库
setuptools>=60.0.0
protobuf>=6.31.1  # 升级到最新稳定版
pyarrow>=20.0.0  # 升级到最新稳定版
python-dateutil==2.9.0.post0
pytz==2025.2

# 其他显式依赖（仅保留你的代码直接调用的库）
tenacity==9.1.2
watchdog==6.0.0
